# API Gateway Service - Routing & Authentication Only
# Request routing, rate limiting, auth validation, monitoring

# Core Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.7.4
pydantic-core>=2.27.0
python-dotenv>=1.0.0
structlog>=25.4.0

# Authentication & Security (validation only, not management)
authlib>=1.2.1
PyJWT>=2.8.0
cryptography>=41.0.0
python-jose>=3.5.0

# Rate limiting & monitoring
slowapi>=0.1.9
prometheus-client>=0.18.0
psutil>=5.9.0

# Database & Session cache (for auth validation)
asyncpg>=0.29.0
redis>=5.0.1
aioredis>=2.0.1

# HTTP routing
httpx>=0.25.0
requests>=2.30.0
python-multipart>=0.0.6

# WebSocket proxying
websockets>=12.0

# CORS middleware (required for frontend integration)
fastapi-cors>=0.0.6

# Configuration
pydantic-settings>=2.0.0
PyYAML>=6.0.0
python-json-logger>=3.3.0
aiofiles>=23.2.1