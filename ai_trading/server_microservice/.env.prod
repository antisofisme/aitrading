# Production Environment Variables
# ===========================================
# SECURITY: Production Configuration
# ===========================================

# Environment
ENVIRONMENT=production
MICROSERVICE_ENVIRONMENT=production

# CORS Configuration (Production Security)
CORS_ENABLED=true
CORS_ORIGINS=https://neliti-trading.com,https://api.neliti-trading.com,https://admin.neliti-trading.com

# Service Authentication
AUTH_ENABLED=true
JWT_SECRET_KEY=super-secret-production-jwt-key-2024
SERVICE_AUTH_ENABLED=true

# API Keys (should be moved to Docker secrets)
# OPENAI_API_KEY=sk-... (moved to secrets)
# DEEPSEEK_API_KEY=sk-... (moved to secrets)
# GOOGLE_AI_API_KEY=... (moved to secrets)
# HANDIT_AI_API_KEY=... (moved to secrets)

# Database Configuration (Production)
POSTGRES_USER=neliti_prod_user
POSTGRES_PASSWORD=neliti_secure_production_password_2024
POSTGRES_DB=neliti_production

CLICKHOUSE_USER=neliti_prod_user
CLICKHOUSE_PASSWORD=clickhouse_secure_production_password_2024
CLICKHOUSE_DB=trading_production_data

# Service Ports (same as development)
API_GATEWAY_PORT=8000
DATA_BRIDGE_PORT=8001
AI_ORCHESTRATION_PORT=8003
DEEP_LEARNING_PORT=8004
AI_PROVIDER_PORT=8005
ML_PROCESSING_PORT=8006
TRADING_ENGINE_PORT=8007
DATABASE_SERVICE_PORT=8008
USER_SERVICE_PORT=8009

# Logging (Production Level)
LOG_LEVEL=INFO
DEBUG=false

# Rate Limiting (Production Values)
RATE_LIMITING_ENABLED=true
RATE_LIMITING_REQUESTS_PER_MINUTE=1000

# WebSocket Configuration
WEBSOCKET_ENABLED=true

# MT5 Configuration (Production)
# MT5_LOGIN=... (moved to secrets)
# MT5_PASSWORD=... (moved to secrets)
MT5_SERVER=MetaQuotes-Live