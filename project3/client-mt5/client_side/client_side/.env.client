# ===================================
# CLIENT SIDE ENVIRONMENT VARIABLES
# ===================================
# Production environment file for client deployment
# Centralized configuration for all client-side components

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
APP_NAME=MT5 Bridge Client
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=false

# =============================================================================
# MT5 CONFIGURATION
# =============================================================================
# MT5 Connection Credentials (REQUIRED - Replace with actual values)
MT5_LOGIN=101632934
MT5_PASSWORD=PtaFs!y4
MT5_SERVER=FBS-Demo

# MT5 Installation Paths
MT5_WINDOWS_PATH=C:\Program Files\MetaTrader 5\terminal64.exe
MT5_LINUX_PATH=/opt/mt5/terminal64
MT5_CUSTOM_PATH=

# MT5 Connection Settings
MT5_LOGIN_TIMEOUT=30.0
MT5_CONNECTION_RETRY_ATTEMPTS=3
MT5_CONNECTION_RETRY_DELAY=5.0
MT5_INITIALIZATION_TIMEOUT=60.0
MT5_STARTUP_WAIT_TIME=3.0

# MT5 Trading Settings
MT5_DEFAULT_MAGIC_NUMBER=234000
MT5_DEFAULT_DEVIATION_POINTS=20
MT5_DEFAULT_ORDER_TIMEOUT=30.0
MT5_ORDER_FILL_TYPE=IOC

# =============================================================================
# SERVER CONNECTION CONFIGURATION - UPDATED FOR API GATEWAY
# =============================================================================
# Server Endpoints (TEMPORARILY: Direct to Data-Bridge until API Gateway WebSocket issue is fixed)
SERVER_HOST=localhost
SERVER_PORT=8001
SERVER_PROTOCOL=http
WEBSOCKET_PROTOCOL=ws

# API Endpoints (TEMPORARILY: Using working Data-Bridge endpoint)
API_BASE_PATH=/api
WEBSOCKET_PATH=/api/v1/ws
HEALTH_PATH=/health

# Connection Timeouts (seconds)
CONNECTION_TIMEOUT=10.0
REQUEST_TIMEOUT=30.0
WEBSOCKET_TIMEOUT=60.0
WEBSOCKET_PING_INTERVAL=20.0
WEBSOCKET_PING_TIMEOUT=10.0

# Retry Settings
MAX_RECONNECT_ATTEMPTS=5
RECONNECT_BACKOFF_BASE=2.0
RECONNECT_MAX_DELAY=60.0

# Heartbeat Settings
HEARTBEAT_INTERVAL=30
HEARTBEAT_TIMEOUT=10.0

# =============================================================================
# STREAMING CONFIGURATION (REDPANDA/KAFKA)
# =============================================================================
# Bootstrap Servers with Fallback
STREAMING_PRIMARY_SERVERS=127.0.0.1:19092,localhost:19092
STREAMING_FALLBACK_SERVERS=127.0.0.1:9092,localhost:9092

# Connection Settings
STREAMING_CLIENT_ID=mt5_bridge
STREAMING_GROUP_ID=mt5_bridge_group
STREAMING_SECURITY_PROTOCOL=PLAINTEXT

# Producer Settings
STREAMING_BATCH_SIZE=16384
STREAMING_LINGER_MS=5
STREAMING_COMPRESSION_TYPE=gzip
STREAMING_ACKS=1
STREAMING_RETRIES=3
STREAMING_RETRY_BACKOFF_MS=100
STREAMING_REQUEST_TIMEOUT_MS=30000

# Consumer Settings
STREAMING_AUTO_OFFSET_RESET=latest
STREAMING_ENABLE_AUTO_COMMIT=true
STREAMING_AUTO_COMMIT_INTERVAL_MS=5000
STREAMING_SESSION_TIMEOUT_MS=30000
STREAMING_HEARTBEAT_INTERVAL_MS=3000

# Topics Configuration
TICK_DATA_TOPIC=mt5_tick_data
EVENTS_TOPIC=mt5_events
RESPONSES_TOPIC=mt5_responses
SIGNALS_TOPIC=trading_signals
ANALYTICS_TOPIC=market_analytics

# Fallback Settings
ENABLE_FALLBACK_WEBSOCKET=true
MAX_CONNECTION_ATTEMPTS=3
CONNECTION_RETRY_DELAY=2.0

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Log Levels and Format
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_FILE=mt5_bridge.log
LOG_MAX_SIZE=10485760
LOG_RETENTION=7
LOG_ROTATION=true

# =============================================================================
# TRADING CONFIGURATION (PRODUCTION SAFE)
# =============================================================================
# Trading Enablement (SAFE DEFAULTS)
TRADING_ENABLED=true
PAPER_TRADING=false
SIMULATION_MODE=false
EMERGENCY_STOP=false

# Risk Management
MAX_RISK_PER_TRADE=1.0
MAX_DAILY_TRADES=5
MAX_POSITION_SIZE=1.0
MAX_TOTAL_EXPOSURE=5.0

# Position Management
DEFAULT_STOP_LOSS_PIPS=50
DEFAULT_TAKE_PROFIT_PIPS=100
MAX_SLIPPAGE_PIPS=3

# =============================================================================
# DATA PROCESSING CONFIGURATION
# =============================================================================
# Data Processing Settings
DATA_PROCESSING_ENABLED=true
DATA_BATCH_SIZE=100
DATA_BATCH_TIMEOUT=5.0

# Performance Settings
REQUEST_BATCH_SIZE=100
REQUEST_BATCH_TIMEOUT=5.0

# =============================================================================
# FILE PATHS CONFIGURATION
# =============================================================================
# Directories
CONFIG_DIR=config
LOGS_DIR=logs
DATA_DIR=data
TEMP_DIR=temp

# =============================================================================
# MAJOR CURRENCY PAIRS CONFIGURATION
# =============================================================================
# Trading Symbols (can be overridden by environment)
MAJOR_PAIRS=EURUSD,GBPUSD,USDJPY,AUDUSD,USDCAD,USDCHF

# Symbol Settings
SYMBOL_SELECTION_TIMEOUT=10.0
TICK_REQUEST_TIMEOUT=5.0